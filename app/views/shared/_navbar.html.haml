%nav
  %ul{ class: 'nav' }
    %li
      %strong Simple Auth App
    - if user_signed_in?
      %li= link_to 'Log out', logout_path, data: { turbo_method: :delete }
    - else
      %li= link_to 'Sign up', sign_up_path
      %li= link_to 'Login', login_path
